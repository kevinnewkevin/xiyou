require "FairyGUI"

function Init(root, objData)
	local com = UIPackage.CreateObject("headbar", "headbar").asCom;
	com.transform.parent = root;
end

function Update(objData)
	
end

function Emit(objData)
	
end