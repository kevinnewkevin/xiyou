INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR}/)


FILE(GLOB_RECURSE HEADER "*.h")
FILE(GLOB_RECURSE SOURCE "*.c")

SOURCE_GROUP(Header FILES ${HEADER})
SOURCE_GROUP(Source FILES ${SOURCE})

ADD_LIBRARY(lua STATIC ${HEADER} ${SOURCE})
