sys.log("SK_265_Action unactive 3")

--主角被动3技能  增加法术防御力20*技能等级


function SK_265_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 265	-- 技能id

	local level = 1
	
	--local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,casterid,level,137,20)

	Battle.TargetOver(battleid)

end
function SK_266_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 266	-- 技能id

	local level = 2
	
	--local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,casterid,level,137,20)

	Battle.TargetOver(battleid)

end
function SK_267_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 267	-- 技能id

	local level = 3
	
	--local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,casterid,level,137,20)

	Battle.TargetOver(battleid)

end
function SK_268_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 268	-- 技能id

	local level = 4
	
	--local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,casterid,level,137,20)

	Battle.TargetOver(battleid)

end
function SK_269_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 269	-- 技能id

	local level = 5
	
	--local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,casterid,level,137,20)

	Battle.TargetOver(battleid)

end
function SK_270_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 270	-- 技能id

	local level = 6
	
	--local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,casterid,level,137,20)

	Battle.TargetOver(battleid)

end
function SK_271_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 271	-- 技能id

	local level = 7
	
	--local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,casterid,level,137,20)

	Battle.TargetOver(battleid)

end
function SK_272_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 272	-- 技能id

	local level = 8
	
	--local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,casterid,level,137,20)

	Battle.TargetOver(battleid)

end
function SK_273_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 273	-- 技能id

	local level = 9
	
	--local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,casterid,level,137,20)

	Battle.TargetOver(battleid)

end
function SK_274_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 274	-- 技能id

	local level = 10
	
	--local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,casterid,level,137,20)

	Battle.TargetOver(battleid)

end
function SK_275_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 275	-- 技能id

	local level = 11
	
	--local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,casterid,level,137,20)

	Battle.TargetOver(battleid)

end
function SK_276_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 276	-- 技能id

	local level = 12
	
	--local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,casterid,level,137,20)

	Battle.TargetOver(battleid)

end
function SK_277_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 277	-- 技能id

	local level = 13
	
	--local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,casterid,level,137,20)

	Battle.TargetOver(battleid)

end
function SK_278_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 278	-- 技能id

	local level = 14
	
	--local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,casterid,level,137,20)

	Battle.TargetOver(battleid)

end
function SK_279_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 279	-- 技能id

	local level = 15
	
	--local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,casterid,level,137,20)

	Battle.TargetOver(battleid)

end
