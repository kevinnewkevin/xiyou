sys.log(" SK_220_Action active 9")

--主角9技能  增加友方单体目标吸血

function SK_220_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 220	-- 技能id

	local level = 1
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,134,5)

	Battle.TargetOver(battleid)

end

function SK_221_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 221	-- 技能id

	local level = 2
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,134,5)

	Battle.TargetOver(battleid)

end
function SK_222_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 222	-- 技能id

	local level = 3
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,134,5)

	Battle.TargetOver(battleid)

end
function SK_223_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 223	-- 技能id

	local level = 4
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,134,5)

	Battle.TargetOver(battleid)

end
function SK_224_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 224-- 技能id

	local level = 5
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,134,5)

	Battle.TargetOver(battleid)

end
function SK_225_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 225	-- 技能id

	local level = 6
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,134,5)

	Battle.TargetOver(battleid)

end
function SK_226_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 226	-- 技能id

	local level = 7
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,134,5)

	Battle.TargetOver(battleid)

end
function SK_227_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 227	-- 技能id

	local level = 8
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,134,5)

	Battle.TargetOver(battleid)

end
function SK_228_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 228	-- 技能id

	local level = 9
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,134,5)

	Battle.TargetOver(battleid)

end
function SK_229_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 229	-- 技能id

	local level = 10
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,134,5)

	Battle.TargetOver(battleid)

end
function SK_230_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 230	-- 技能id

	local level = 11
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,134,5)

	Battle.TargetOver(battleid)

end
function SK_231_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 231	-- 技能id

	local level = 12
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,134,5)

	Battle.TargetOver(battleid)

end
function SK_232_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 232	-- 技能id

	local level = 13
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,134,5)

	Battle.TargetOver(battleid)

end
function SK_233_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 233	-- 技能id

	local level = 14
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,134,5)

	Battle.TargetOver(battleid)

end
function SK_234_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 234	-- 技能id

	local level = 15
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,134,5)

	Battle.TargetOver(battleid)

end
