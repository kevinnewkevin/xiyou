sys.log(" skill 49 start")

--主角被动3技能  增加法术防御力20*技能等级


function SK_148_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 148	-- 技能id

	local level = 1
	
	--local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,casterid,level,137,20)

	Battle.TargetOver(battleid)

end
function SK_323_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 323	-- 技能id

	local level = 2
	
	--local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,casterid,level,137,20)

	Battle.TargetOver(battleid)

end
function SK_324_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 324	-- 技能id

	local level = 3
	
	--local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,casterid,level,137,20)

	Battle.TargetOver(battleid)

end
function SK_325_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 325	-- 技能id

	local level = 4
	
	--local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,casterid,level,137,20)

	Battle.TargetOver(battleid)

end
function SK_326_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 326	-- 技能id

	local level = 5
	
	--local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,casterid,level,137,20)

	Battle.TargetOver(battleid)

end
function SK_327_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 327	-- 技能id

	local level = 6
	
	--local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,casterid,level,137,20)

	Battle.TargetOver(battleid)

end
function SK_328_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 328	-- 技能id

	local level = 7
	
	--local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,casterid,level,137,20)

	Battle.TargetOver(battleid)

end
function SK_329_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 329	-- 技能id

	local level = 8
	
	--local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,casterid,level,137,20)

	Battle.TargetOver(battleid)

end
function SK_330_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 330	-- 技能id

	local level = 9
	
	--local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,casterid,level,137,20)

	Battle.TargetOver(battleid)

end
function SK_331_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 331	-- 技能id

	local level = 10
	
	--local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,casterid,level,137,20)

	Battle.TargetOver(battleid)

end
function SK_332_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 332	-- 技能id

	local level = 11
	
	--local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,casterid,level,137,20)

	Battle.TargetOver(battleid)

end
function SK_333_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 333	-- 技能id

	local level = 12
	
	--local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,casterid,level,137,20)

	Battle.TargetOver(battleid)

end
function SK_334_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 334	-- 技能id

	local level = 13
	
	--local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,casterid,level,137,20)

	Battle.TargetOver(battleid)

end
function SK_335_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 335	-- 技能id

	local level = 14
	
	--local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,casterid,level,137,20)

	Battle.TargetOver(battleid)

end
function SK_336_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 336	-- 技能id

	local level = 15
	
	--local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,casterid,level,137,20)

	Battle.TargetOver(battleid)

end
sys.log(" skill 49 end")