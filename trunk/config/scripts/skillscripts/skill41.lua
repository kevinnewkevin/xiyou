sys.log(" skill 41 start")

--主角4技能  增加友方单体目标暴击伤害

function SK_140_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 140	-- 技能id

	local level = 1
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,129,5)

	Battle.TargetOver(battleid)

end

function SK_197_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 197	-- 技能id

	local level = 2
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,129,5)

	Battle.TargetOver(battleid)

end
function SK_198_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 198	-- 技能id

	local level = 3
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,129,5)

	Battle.TargetOver(battleid)

end
function SK_199_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 199	-- 技能id

	local level = 4
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,129,5)

	Battle.TargetOver(battleid)

end
function SK_200_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 200	-- 技能id

	local level = 5
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,129,5)

	Battle.TargetOver(battleid)

end
function SK_201_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 201	-- 技能id

	local level = 6
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,129,5)

	Battle.TargetOver(battleid)

end
function SK_202_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 202	-- 技能id

	local level = 7
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,129,5)

	Battle.TargetOver(battleid)

end
function SK_202_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 202	-- 技能id

	local level = 8
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,129,5)

	Battle.TargetOver(battleid)

end
function SK_203_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 203	-- 技能id

	local level = 9
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,129,5)

	Battle.TargetOver(battleid)

end
function SK_204_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 204	-- 技能id

	local level = 10
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,129,5)

	Battle.TargetOver(battleid)

end
function SK_205_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 205	-- 技能id

	local level = 11
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,129,5)

	Battle.TargetOver(battleid)

end
function SK_206_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 206	-- 技能id

	local level = 12
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,129,5)

	Battle.TargetOver(battleid)

end
function SK_207_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 207	-- 技能id

	local level = 13
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,129,5)

	Battle.TargetOver(battleid)

end
function SK_208_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 208	-- 技能id

	local level = 14
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,129,5)

	Battle.TargetOver(battleid)

end
function SK_209_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 209	-- 技能id

	local level = 15
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,129,5)

	Battle.TargetOver(battleid)

end

sys.log(" skill 42 end")