sys.log(" skill 45 start")

--主角8技能  增加友方单体目标荆棘

function SK_144_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 144	-- 技能id

	local level = 1
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,133,5)

	Battle.TargetOver(battleid)

end
function SK_253_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 253	-- 技能id

	local level = 2
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,133,5)

	Battle.TargetOver(battleid)

end
function SK_254_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 254	-- 技能id

	local level = 3
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,133,5)

	Battle.TargetOver(battleid)

end
function SK_255_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 255	-- 技能id

	local level = 4
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,133,5)

	Battle.TargetOver(battleid)

end
function SK_256_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 256	-- 技能id

	local level = 5
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,133,5)

	Battle.TargetOver(battleid)

end
function SK_257_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 257	-- 技能id

	local level = 6
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,133,5)

	Battle.TargetOver(battleid)

end
function SK_258_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 258	-- 技能id

	local level = 7
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,133,5)

	Battle.TargetOver(battleid)

end
function SK_259_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 259	-- 技能id

	local level = 8
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,133,5)

	Battle.TargetOver(battleid)

end
function SK_260_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 260	-- 技能id

	local level = 9
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,133,5)

	Battle.TargetOver(battleid)

end
function SK_261_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 261	-- 技能id

	local level = 10
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,133,5)

	Battle.TargetOver(battleid)

end
function SK_262_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 262	-- 技能id

	local level = 11
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,133,5)

	Battle.TargetOver(battleid)

end
function SK_263_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 263	-- 技能id

	local level = 12
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,133,5)

	Battle.TargetOver(battleid)

end
function SK_264_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 264	-- 技能id

	local level = 13
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,133,5)

	Battle.TargetOver(battleid)

end
function SK_265_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 265	-- 技能id

	local level = 14
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,133,5)

	Battle.TargetOver(battleid)

end
function SK_266_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 266	-- 技能id

	local level = 15
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,133,5)

	Battle.TargetOver(battleid)

end

sys.log(" skill 45 end")