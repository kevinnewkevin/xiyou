sys.log(" skill 44 start")

--主角7技能  增加友方单体目标回复

function SK_143_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 143-- 技能id

	local level = 1
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,132,5)

	Battle.TargetOver(battleid)

end

function SK_1239_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 239-- 技能id

	local level = 2
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,132,5)

	Battle.TargetOver(battleid)

end

function SK_240_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 240-- 技能id

	local level = 3
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,132,5)

	Battle.TargetOver(battleid)

end

function SK_241_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 241- 技能id

	local level = 4
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,132,5)

	Battle.TargetOver(battleid)

end

function SK_242_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 242-- 技能id

	local level = 5
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,132,5)

	Battle.TargetOver(battleid)

end

function SK_243_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 243-- 技能id

	local level = 6
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,132,5)

	Battle.TargetOver(battleid)

end

function SK_244_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 244-- 技能id

	local level = 7
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,132,5)

	Battle.TargetOver(battleid)

end

function SK_245_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 245-- 技能id

	local level = 8
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,132,5)

	Battle.TargetOver(battleid)

end

function SK_246_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 246-- 技能id

	local level = 9
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,132,5)

	Battle.TargetOver(battleid)

end

function SK_247_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 247-- 技能id

	local level = 10
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,132,5)

	Battle.TargetOver(battleid)

end

function SK_248_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 248- 技能id

	local level = 11
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,132,5)

	Battle.TargetOver(battleid)

end

function SK_249_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 249-- 技能id

	local level = 12
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,132,5)

	Battle.TargetOver(battleid)

end

function SK_250_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 250-- 技能id

	local level = 13
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,132,5)

	Battle.TargetOver(battleid)

end

function SK_251_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 251-- 技能id

	local level = 14
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,132,5)

	Battle.TargetOver(battleid)

end

function SK_252_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 252-- 技能id

	local level = 15
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,132,5)

	Battle.TargetOver(battleid)

end

sys.log(" skill 44 end")