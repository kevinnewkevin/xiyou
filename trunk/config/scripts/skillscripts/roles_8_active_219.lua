sys.log(" 主角 主动技能 8 开始")

--主角8技能  增加友方单体目标荆棘

function SK_205_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 205	-- 技能id

	local level = 1
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,133,5)

	Battle.TargetOver(battleid)

end
function SK_206_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 206	-- 技能id

	local level = 2
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,133,5)

	Battle.TargetOver(battleid)

end
function SK_207_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 207	-- 技能id

	local level = 3
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,133,5)

	Battle.TargetOver(battleid)

end
function SK_208_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 208	-- 技能id

	local level = 4
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,133,5)

	Battle.TargetOver(battleid)

end
function SK_209_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 209	-- 技能id

	local level = 5
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,133,5)

	Battle.TargetOver(battleid)

end
function SK_210_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 210	-- 技能id

	local level = 6
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,133,5)

	Battle.TargetOver(battleid)

end
function SK_211_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 211	-- 技能id

	local level = 7
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,133,5)

	Battle.TargetOver(battleid)

end
function SK_212_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 212	-- 技能id

	local level = 8
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,133,5)

	Battle.TargetOver(battleid)

end
function SK_213_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 213	-- 技能id

	local level = 9
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,133,5)

	Battle.TargetOver(battleid)

end
function SK_214_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 214	-- 技能id

	local level = 10
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,133,5)

	Battle.TargetOver(battleid)

end
function SK_215_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 215	-- 技能id

	local level = 11
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,133,5)

	Battle.TargetOver(battleid)

end
function SK_216_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 216	-- 技能id

	local level = 12
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,133,5)

	Battle.TargetOver(battleid)

end
function SK_217_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 217	-- 技能id

	local level = 13
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,133,5)

	Battle.TargetOver(battleid)

end
function SK_218_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 218	-- 技能id

	local level = 14
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,133,5)

	Battle.TargetOver(battleid)

end
function SK_219_Action(battleid, casterid)
	Battle.TargetOn(battleid)
	
	local skillid = 219	-- 技能id

	local level = 15
	
	local  t = Player.GetFriend(battleid,casterid)  --获取目标

	skill_user_atk(battleid,casterid,t,level,133,5)

	Battle.TargetOver(battleid)

end
sys.log(" 主角 主动技能 8 结束")